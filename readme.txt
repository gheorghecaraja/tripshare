# Installing TripShare

1. Create a MySQL database (name: tripshare)
2. Import the SQL file from /database/tripshare.sql into phpMyAdmin
3. Edit config.php and add your database connection details
4. Run the project on localhost using XAMPP
